//! OLM v0 (`operators.coreos.com`): the objects, how they join into installed operators, the
//! upgrade review, OperatorHub, installs and uninstalls. OLM v1 in [`v1`].

pub mod hub;
pub mod join;
pub mod model;
pub mod ops;
pub mod review;
pub mod v1;
